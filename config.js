// Fill these in from your Supabase project (Project Settings → API), then
// upload this file along with everything else. The anon key is safe to
// expose in client-side code by design — access to your data is enforced
// server-side by Row Level Security, not by keeping this key secret.
export const SUPABASE_URL = 'YOUR_SUPABASE_PROJECT_URL';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
